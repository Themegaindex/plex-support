---
import Layout from '../layouts/Layout.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Layout title="Kontakt - Plex Support" description="Kontaktiere das Plex Support Support-Team per E-Mail oder Kontaktformular.">
  <div class="page-header">
    <div class="container">
      <h1>Kontakt</h1>
      <p>Wir helfen dir gerne weiter. Schreib uns eine E-Mail oder nutze das Kontaktformular.</p>
    </div>
  </div>

  <section class="section">
    <div class="container">
      <div class="contact-grid">
        <div class="contact-info">
          <div class="contact-card card">
            <div class="contact-card-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="2" y="4" width="20" height="16" rx="2"/>
                <path d="M22 4L12 13L2 4"/>
              </svg>
            </div>
            <h2>E-Mail Support</h2>
            <p>Der schnellste Weg, uns zu erreichen:</p>
            <a href="mailto:plex.hilfe24@gmail.com" class="email-link">plex.hilfe24@gmail.com</a>
          </div>

          <div class="support-scope card">
            <h3>Bevor du schreibst</h3>
            <ul>
              <li>Schaue in den <a href={`${base}/faq`}>FAQ</a> nach, ob deine Frage schon beantwortet wurde</li>
              <li>Prüfe die <a href={`${base}/hilfe`}>Hilfe-Seite</a> für Fehlerbehebung</li>
              <li>Beschreibe dein Problem so genau wie möglich</li>
              <li>Nenne dein Gerät und die verwendete App</li>
            </ul>
          </div>
        </div>

        <div class="contact-form-wrapper card">
          <h2>Kontaktformular</h2>
          <p class="form-desc">Fülle das Formular aus und wir melden uns so schnell wie möglich.</p>

          <form id="contact-form" novalidate>
            <input type="hidden" name="access_key" value="DEIN_ACCESS_KEY" />
            <input type="hidden" name="subject" value="Neue Nachricht über Plex Support Kontaktformular" />
            <input type="hidden" name="from_name" value="Plex Support Website" />
            <div class="form-group">
              <label for="name">Name *</label>
              <input type="text" id="name" name="name" required autocomplete="name" placeholder="Dein Name" />
              <span class="form-error" id="name-error"></span>
            </div>

            <div class="form-group">
              <label for="email">E-Mail *</label>
              <input type="email" id="email" name="email" required autocomplete="email" placeholder="deine@email.de" />
              <span class="form-error" id="email-error"></span>
            </div>

            <div class="form-group">
              <label for="subject">Betreff *</label>
              <select id="subject" name="subject" required>
                <option value="" disabled selected>Bitte wählen...</option>
                <option value="Streaming-Problem">Streaming-Problem</option>
                <option value="Konto-Problem">Konto-Problem</option>
                <option value="Zahlungsfrage">Zahlungsfrage</option>
                <option value="Allgemeine Frage">Allgemeine Frage</option>
                <option value="Sonstiges">Sonstiges</option>
              </select>
              <span class="form-error" id="subject-error"></span>
            </div>

            <div class="form-group">
              <label for="message">Nachricht *</label>
              <textarea id="message" name="message" rows="5" required placeholder="Beschreibe dein Anliegen so detailliert wie möglich..." minlength="20"></textarea>
              <span class="form-error" id="message-error"></span>
            </div>

            <button type="submit" class="btn btn-primary btn-full" id="submit-btn">
              <span class="btn-text">Nachricht senden</span>
              <span class="btn-loading" style="display:none;">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="spinner">
                  <path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/>
                </svg>
                Wird gesendet...
              </span>
            </button>
          </form>

          <div class="form-success" id="form-success" style="display:none;">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
            <h3>Nachricht gesendet!</h3>
            <p>Wir haben deine Nachricht erhalten und melden uns so schnell wie möglich.</p>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: var(--spacing-xl);
    align-items: start;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .contact-card {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-md);
  }

  .contact-card-icon {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-lg);
    background: var(--color-primary-muted);
    color: var(--color-primary-hover);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contact-card h2 {
    font-size: 1.2rem;
  }

  .contact-card p {
    font-size: 0.9rem;
  }

  .email-link {
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--color-primary-hover);
    padding: 8px 20px;
    background: var(--color-primary-muted);
    border-radius: var(--radius-md);
    transition: all var(--transition-fast);
  }

  .email-link:hover {
    background: var(--color-primary);
    color: #fff;
  }

  .support-scope h3 {
    font-size: 1rem;
    margin-bottom: var(--spacing-md);
  }

  .support-scope ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .support-scope li {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    padding-left: var(--spacing-lg);
    position: relative;
  }

  .support-scope li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 7px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-primary);
  }

  .contact-form-wrapper h2 {
    font-size: 1.2rem;
    margin-bottom: var(--spacing-xs);
  }

  .form-desc {
    font-size: 0.9rem;
    margin-bottom: var(--spacing-lg);
  }

  .form-group {
    margin-bottom: var(--spacing-md);
  }

  .form-group label {
    display: block;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--color-text-secondary);
    margin-bottom: 6px;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 10px 14px;
    background: var(--color-bg-input);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    color: var(--color-text);
    font-family: var(--font-sans);
    font-size: 0.9rem;
    outline: none;
    transition: border-color var(--transition-fast);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: var(--color-text-muted);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    border-color: var(--color-accent-hover);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .form-group select {
    cursor: pointer;
    appearance: none;
    background-image: url("data:image/svg+xml,%3Csvg width='12' height='8' viewBox='0 0 12 8' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1L6 6L11 1' stroke='%236b7280' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
    background-repeat: no-repeat;
    background-position: right 14px center;
    padding-right: 40px;
  }

  .form-group select option {
    background: var(--color-bg-secondary);
    color: var(--color-text);
  }

  .form-error {
    display: none;
    font-size: 0.8rem;
    color: var(--color-error);
    margin-top: 4px;
  }

  .form-group.error input,
  .form-group.error select,
  .form-group.error textarea {
    border-color: var(--color-error);
  }

  .form-group.error .form-error {
    display: block;
  }

  .btn-full {
    width: 100%;
    justify-content: center;
  }

  .btn-loading {
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .spinner {
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .form-success {
    text-align: center;
    padding: var(--spacing-xl);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-md);
    color: var(--color-primary-hover);
  }

  .form-success h3 {
    color: var(--color-primary-hover);
  }

  .form-success p {
    font-size: 0.9rem;
    max-width: 400px;
  }

  @media (max-width: 768px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const submitBtn = document.getElementById('submit-btn');
  const btnText = submitBtn?.querySelector('.btn-text') as HTMLElement;
  const btnLoading = submitBtn?.querySelector('.btn-loading') as HTMLElement;
  const successMsg = document.getElementById('form-success');

  function validateField(id: string, errorMsg: string): boolean {
    const input = document.getElementById(id) as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement;
    const group = input.closest('.form-group');
    const errorEl = document.getElementById(`${id}-error`);

    if (!input.value.trim() || (input.type === 'email' && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(input.value))) {
      group?.classList.add('error');
      if (errorEl) errorEl.textContent = errorMsg;
      return false;
    }

    if (id === 'message' && input.value.trim().length < 20) {
      group?.classList.add('error');
      if (errorEl) errorEl.textContent = 'Bitte mindestens 20 Zeichen eingeben.';
      return false;
    }

    group?.classList.remove('error');
    return true;
  }

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const validName = validateField('name', 'Bitte gib deinen Namen ein.');
    const validEmail = validateField('email', 'Bitte gib eine gültige E-Mail-Adresse ein.');
    const validSubject = validateField('subject', 'Bitte wähle einen Betreff.');
    const validMessage = validateField('message', 'Bitte beschreibe dein Anliegen.');

    if (!validName || !validEmail || !validSubject || !validMessage) return;

    if (btnText) btnText.style.display = 'none';
    if (btnLoading) btnLoading.style.display = 'inline-flex';
    if (submitBtn) (submitBtn as HTMLButtonElement).disabled = true;

    try {
      const data = new FormData(form);
      const response = await fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        body: data,
      });

      const result = await response.json();

      if (result.success) {
        if (form) form.style.display = 'none';
        if (successMsg) successMsg.style.display = 'flex';
      } else {
        alert('Fehler beim Senden. Bitte schreib uns direkt an plex.hilfe24@gmail.com');
        if (btnText) btnText.style.display = 'inline';
        if (btnLoading) btnLoading.style.display = 'none';
        if (submitBtn) (submitBtn as HTMLButtonElement).disabled = false;
      }
    } catch {
      alert('Fehler beim Senden. Bitte schreib uns direkt an plex.hilfe24@gmail.com');
      if (btnText) btnText.style.display = 'inline';
      if (btnLoading) btnLoading.style.display = 'none';
      if (submitBtn) (submitBtn as HTMLButtonElement).disabled = false;
    }
  });

  ['name', 'email', 'subject', 'message'].forEach(id => {
    const el = document.getElementById(id);
    el?.addEventListener('input', () => {
      el.closest('.form-group')?.classList.remove('error');
    });
  });
</script>
